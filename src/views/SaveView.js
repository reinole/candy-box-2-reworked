import React from 'react';
import ViewWrapper from '../components/ViewWrapper';

const SaveView = () => {
	return (
		<ViewWrapper>
			<h1>Saving</h1>
			<span>The game automatically saves</span>

			<h3>If you wish to start over, i should create a delete function</h3>
		</ViewWrapper>
	);
};

// const mapStateToProps = () => {
// 	return {
// 		state: state
// 	};
// };

export default SaveView;
